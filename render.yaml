services:
  - type: web
    name: hiring-approvals
    env: node
    plan: free
    buildCommand: npm run build:render
    startCommand: node backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
